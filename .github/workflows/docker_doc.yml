name: Documentation check

on:
  pull_request:
    paths:
      - "README.md"
      - ".github/workflows/docker_doc.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify required documentation
        run: |
          test -s README.md
          grep -q "COMWECHAT_BRIDGE_ENABLED" README.md
          grep -q "回滚" README.md
